[pytest]
testpaths = agent/tests backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: Integration tests (require full setup)
    unit: Unit tests (isolated, fast)
addopts = -v --strict-markers

